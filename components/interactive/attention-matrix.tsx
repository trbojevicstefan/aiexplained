"use client";

type Props={tokens:string[];weights:number[][];activeRow?:number;onRowSelect?:(row:number)=>void;masked?:boolean[][]};
export function AttentionMatrix({tokens,weights,activeRow=0,onRowSelect,masked}:Props){return <div style={{overflowX:"auto"}}><div style={{display:"grid",gridTemplateColumns:`110px repeat(${tokens.length},minmax(64px,1fr))`,gap:4,minWidth:110+tokens.length*68}}><div/><>{tokens.map((t,i)=><div key={`h-${i}`} style={{fontWeight:850,textAlign:"center",padding:7}}>{t}</div>)}</>{tokens.map((rowToken,r)=><span key={`row-${r}`} style={{display:"contents"}}><button onClick={()=>onRowSelect?.(r)} style={{border:"2px solid #15130f",borderRadius:10,background:r===activeRow?"#c9f66d":"#fff",fontWeight:850,padding:7,cursor:"pointer"}}>{rowToken} →</button>{tokens.map((_,c)=>{const value=weights[r]?.[c]??0;const isMasked=masked?.[r]?.[c];return <div key={`${r}-${c}`} title={isMasked?"masked":`${(value*100).toFixed(1)}%`} style={{border:"2px solid #15130f",borderRadius:8,padding:"12px 6px",textAlign:"center",fontFamily:"ui-monospace,monospace",background:isMasked?"repeating-linear-gradient(135deg,#ddd,#ddd 6px,#aaa 6px,#aaa 12px)":`rgba(112,91,230,${Math.max(.08,value)})`,color:value>.55&&!isMasked?"#fff":"#15130f"}}>{isMasked?"×":value.toFixed(2)}</div>})}</span>)}</div></div>}
